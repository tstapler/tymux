// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tymux/v1/tymux.proto

package tymuxv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/tstapler/tymux/clients/go/gen/tymux/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TymuxServiceName is the fully-qualified name of the TymuxService service.
	TymuxServiceName = "tymux.v1.TymuxService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TymuxServiceCreateSessionProcedure is the fully-qualified name of the TymuxService's
	// CreateSession RPC.
	TymuxServiceCreateSessionProcedure = "/tymux.v1.TymuxService/CreateSession"
	// TymuxServiceListSessionsProcedure is the fully-qualified name of the TymuxService's ListSessions
	// RPC.
	TymuxServiceListSessionsProcedure = "/tymux.v1.TymuxService/ListSessions"
	// TymuxServiceKillSessionProcedure is the fully-qualified name of the TymuxService's KillSession
	// RPC.
	TymuxServiceKillSessionProcedure = "/tymux.v1.TymuxService/KillSession"
	// TymuxServiceReviveSessionProcedure is the fully-qualified name of the TymuxService's
	// ReviveSession RPC.
	TymuxServiceReviveSessionProcedure = "/tymux.v1.TymuxService/ReviveSession"
	// TymuxServiceCapturePaneProcedure is the fully-qualified name of the TymuxService's CapturePane
	// RPC.
	TymuxServiceCapturePaneProcedure = "/tymux.v1.TymuxService/CapturePane"
	// TymuxServiceSearchScrollbackProcedure is the fully-qualified name of the TymuxService's
	// SearchScrollback RPC.
	TymuxServiceSearchScrollbackProcedure = "/tymux.v1.TymuxService/SearchScrollback"
	// TymuxServiceSplitPaneProcedure is the fully-qualified name of the TymuxService's SplitPane RPC.
	TymuxServiceSplitPaneProcedure = "/tymux.v1.TymuxService/SplitPane"
	// TymuxServiceClosePaneProcedure is the fully-qualified name of the TymuxService's ClosePane RPC.
	TymuxServiceClosePaneProcedure = "/tymux.v1.TymuxService/ClosePane"
	// TymuxServiceCreateWindowProcedure is the fully-qualified name of the TymuxService's CreateWindow
	// RPC.
	TymuxServiceCreateWindowProcedure = "/tymux.v1.TymuxService/CreateWindow"
	// TymuxServiceWatchWindowProcedure is the fully-qualified name of the TymuxService's WatchWindow
	// RPC.
	TymuxServiceWatchWindowProcedure = "/tymux.v1.TymuxService/WatchWindow"
	// TymuxServiceAttachProcedure is the fully-qualified name of the TymuxService's Attach RPC.
	TymuxServiceAttachProcedure = "/tymux.v1.TymuxService/Attach"
)

// TymuxServiceClient is a client for the tymux.v1.TymuxService service.
type TymuxServiceClient interface {
	CreateSession(context.Context, *connect.Request[v1.CreateSessionRequest]) (*connect.Response[v1.Session], error)
	ListSessions(context.Context, *connect.Request[v1.ListSessionsRequest]) (*connect.Response[v1.ListSessionsResponse], error)
	KillSession(context.Context, *connect.Request[v1.KillSessionRequest]) (*connect.Response[v1.KillSessionResponse], error)
	// Respawns fresh ptys for a dead-flagged (restored-but-not-yet-revived)
	// session, matching its persisted Layout shape — each pane's original
	// command re-run in its persisted cwd. Never triggered automatically on
	// daemon start (ADR-002); only an explicit call reaches this. A no-op
	// (ReviveSessionResponse.already_live=true) if the session is already
	// live — never a duplicate spawn.
	ReviveSession(context.Context, *connect.Request[v1.ReviveSessionRequest]) (*connect.Response[v1.ReviveSessionResponse], error)
	// One-shot structured read of a pane's current screen — the AI-friendly
	// alternative to `tmux capture-pane` (which returns raw text you'd have
	// to re-parse). Cells carry attributes directly. scrollback_offset lets
	// a caller read a historical screen (copy-mode's navigation primitive)
	// instead of the live one.
	CapturePane(context.Context, *connect.Request[v1.CapturePaneRequest]) (*connect.Response[v1.PaneSnapshot], error)
	// Forward-only substring search through a pane's scrollback — the
	// direct, non-interactive access path for programmatic/agent callers
	// (one shared history buffer, two access paths: this and CapturePane's
	// scrollback_offset).
	SearchScrollback(context.Context, *connect.Request[v1.SearchScrollbackRequest]) (*connect.Response[v1.SearchScrollbackResponse], error)
	// Splits an existing pane into two, nesting a new Split node in its
	// window's Layout tree in the target pane's place. Rejected with
	// failed_precondition (naming the actual/minimum rows+cols) if the
	// resulting leaf would fall below the daemon's hard minimum pane size.
	SplitPane(context.Context, *connect.Request[v1.SplitPaneRequest]) (*connect.Response[v1.Session], error)
	// Closes one pane. If it was the last pane in its window, the window
	// itself closes (cascading to KillSession if it was also the session's
	// last window) — see ClosePaneResponse.window_closed/session_closed.
	ClosePane(context.Context, *connect.Request[v1.ClosePaneRequest]) (*connect.Response[v1.ClosePaneResponse], error)
	// Adds a new window (single pane) to an existing session.
	CreateWindow(context.Context, *connect.Request[v1.CreateWindowRequest]) (*connect.Response[v1.Session], error)
	// Streams the subscribed window's Layout every time its structure or
	// geometry changes, so a client's local model stays in sync without
	// polling ListSessions.
	WatchWindow(context.Context, *connect.Request[v1.WatchWindowRequest]) (*connect.ServerStreamForClient[v1.WindowLayoutEvent], error)
	// Bidirectional stream: client sends input/resize events, server streams
	// output back. This is what an interactive attach (human or agent) uses.
	//
	// Contract:
	//   - The first AttachRequest on the stream MUST set the pane_id field of
	//     the oneof; any other first message is rejected with
	//     invalid_argument.
	//   - Detaching means fully cancelling this call (dropping/closing both
	//     directions). Half-closing only the send side does not stop the
	//     receive side or end the attach — the server keeps streaming output
	//     until the whole call is cancelled.
	//   - output_gap=true on an AttachEvent means the server's internal
	//     output broadcast channel dropped frames for this consumer (it fell
	//     behind); some output between the previous and next Output event was
	//     lost. This is not a fatal condition — normal streaming resumes
	//     immediately after.
	//   - Resize is now window-scoped, not pane-scoped (ADR-004): the
	//     daemon tracks every attached client's reported viewport per window
	//     and applies the dimension-wise minimum across all of them, rather
	//     than sizing this one pane to this one client's last report.
	Attach(context.Context) *connect.BidiStreamForClient[v1.AttachRequest, v1.AttachEvent]
}

// NewTymuxServiceClient constructs a client for the tymux.v1.TymuxService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTymuxServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TymuxServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	tymuxServiceMethods := v1.File_tymux_v1_tymux_proto.Services().ByName("TymuxService").Methods()
	return &tymuxServiceClient{
		createSession: connect.NewClient[v1.CreateSessionRequest, v1.Session](
			httpClient,
			baseURL+TymuxServiceCreateSessionProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("CreateSession")),
			connect.WithClientOptions(opts...),
		),
		listSessions: connect.NewClient[v1.ListSessionsRequest, v1.ListSessionsResponse](
			httpClient,
			baseURL+TymuxServiceListSessionsProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("ListSessions")),
			connect.WithClientOptions(opts...),
		),
		killSession: connect.NewClient[v1.KillSessionRequest, v1.KillSessionResponse](
			httpClient,
			baseURL+TymuxServiceKillSessionProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("KillSession")),
			connect.WithClientOptions(opts...),
		),
		reviveSession: connect.NewClient[v1.ReviveSessionRequest, v1.ReviveSessionResponse](
			httpClient,
			baseURL+TymuxServiceReviveSessionProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("ReviveSession")),
			connect.WithClientOptions(opts...),
		),
		capturePane: connect.NewClient[v1.CapturePaneRequest, v1.PaneSnapshot](
			httpClient,
			baseURL+TymuxServiceCapturePaneProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("CapturePane")),
			connect.WithClientOptions(opts...),
		),
		searchScrollback: connect.NewClient[v1.SearchScrollbackRequest, v1.SearchScrollbackResponse](
			httpClient,
			baseURL+TymuxServiceSearchScrollbackProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("SearchScrollback")),
			connect.WithClientOptions(opts...),
		),
		splitPane: connect.NewClient[v1.SplitPaneRequest, v1.Session](
			httpClient,
			baseURL+TymuxServiceSplitPaneProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("SplitPane")),
			connect.WithClientOptions(opts...),
		),
		closePane: connect.NewClient[v1.ClosePaneRequest, v1.ClosePaneResponse](
			httpClient,
			baseURL+TymuxServiceClosePaneProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("ClosePane")),
			connect.WithClientOptions(opts...),
		),
		createWindow: connect.NewClient[v1.CreateWindowRequest, v1.Session](
			httpClient,
			baseURL+TymuxServiceCreateWindowProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("CreateWindow")),
			connect.WithClientOptions(opts...),
		),
		watchWindow: connect.NewClient[v1.WatchWindowRequest, v1.WindowLayoutEvent](
			httpClient,
			baseURL+TymuxServiceWatchWindowProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("WatchWindow")),
			connect.WithClientOptions(opts...),
		),
		attach: connect.NewClient[v1.AttachRequest, v1.AttachEvent](
			httpClient,
			baseURL+TymuxServiceAttachProcedure,
			connect.WithSchema(tymuxServiceMethods.ByName("Attach")),
			connect.WithClientOptions(opts...),
		),
	}
}

// tymuxServiceClient implements TymuxServiceClient.
type tymuxServiceClient struct {
	createSession    *connect.Client[v1.CreateSessionRequest, v1.Session]
	listSessions     *connect.Client[v1.ListSessionsRequest, v1.ListSessionsResponse]
	killSession      *connect.Client[v1.KillSessionRequest, v1.KillSessionResponse]
	reviveSession    *connect.Client[v1.ReviveSessionRequest, v1.ReviveSessionResponse]
	capturePane      *connect.Client[v1.CapturePaneRequest, v1.PaneSnapshot]
	searchScrollback *connect.Client[v1.SearchScrollbackRequest, v1.SearchScrollbackResponse]
	splitPane        *connect.Client[v1.SplitPaneRequest, v1.Session]
	closePane        *connect.Client[v1.ClosePaneRequest, v1.ClosePaneResponse]
	createWindow     *connect.Client[v1.CreateWindowRequest, v1.Session]
	watchWindow      *connect.Client[v1.WatchWindowRequest, v1.WindowLayoutEvent]
	attach           *connect.Client[v1.AttachRequest, v1.AttachEvent]
}

// CreateSession calls tymux.v1.TymuxService.CreateSession.
func (c *tymuxServiceClient) CreateSession(ctx context.Context, req *connect.Request[v1.CreateSessionRequest]) (*connect.Response[v1.Session], error) {
	return c.createSession.CallUnary(ctx, req)
}

// ListSessions calls tymux.v1.TymuxService.ListSessions.
func (c *tymuxServiceClient) ListSessions(ctx context.Context, req *connect.Request[v1.ListSessionsRequest]) (*connect.Response[v1.ListSessionsResponse], error) {
	return c.listSessions.CallUnary(ctx, req)
}

// KillSession calls tymux.v1.TymuxService.KillSession.
func (c *tymuxServiceClient) KillSession(ctx context.Context, req *connect.Request[v1.KillSessionRequest]) (*connect.Response[v1.KillSessionResponse], error) {
	return c.killSession.CallUnary(ctx, req)
}

// ReviveSession calls tymux.v1.TymuxService.ReviveSession.
func (c *tymuxServiceClient) ReviveSession(ctx context.Context, req *connect.Request[v1.ReviveSessionRequest]) (*connect.Response[v1.ReviveSessionResponse], error) {
	return c.reviveSession.CallUnary(ctx, req)
}

// CapturePane calls tymux.v1.TymuxService.CapturePane.
func (c *tymuxServiceClient) CapturePane(ctx context.Context, req *connect.Request[v1.CapturePaneRequest]) (*connect.Response[v1.PaneSnapshot], error) {
	return c.capturePane.CallUnary(ctx, req)
}

// SearchScrollback calls tymux.v1.TymuxService.SearchScrollback.
func (c *tymuxServiceClient) SearchScrollback(ctx context.Context, req *connect.Request[v1.SearchScrollbackRequest]) (*connect.Response[v1.SearchScrollbackResponse], error) {
	return c.searchScrollback.CallUnary(ctx, req)
}

// SplitPane calls tymux.v1.TymuxService.SplitPane.
func (c *tymuxServiceClient) SplitPane(ctx context.Context, req *connect.Request[v1.SplitPaneRequest]) (*connect.Response[v1.Session], error) {
	return c.splitPane.CallUnary(ctx, req)
}

// ClosePane calls tymux.v1.TymuxService.ClosePane.
func (c *tymuxServiceClient) ClosePane(ctx context.Context, req *connect.Request[v1.ClosePaneRequest]) (*connect.Response[v1.ClosePaneResponse], error) {
	return c.closePane.CallUnary(ctx, req)
}

// CreateWindow calls tymux.v1.TymuxService.CreateWindow.
func (c *tymuxServiceClient) CreateWindow(ctx context.Context, req *connect.Request[v1.CreateWindowRequest]) (*connect.Response[v1.Session], error) {
	return c.createWindow.CallUnary(ctx, req)
}

// WatchWindow calls tymux.v1.TymuxService.WatchWindow.
func (c *tymuxServiceClient) WatchWindow(ctx context.Context, req *connect.Request[v1.WatchWindowRequest]) (*connect.ServerStreamForClient[v1.WindowLayoutEvent], error) {
	return c.watchWindow.CallServerStream(ctx, req)
}

// Attach calls tymux.v1.TymuxService.Attach.
func (c *tymuxServiceClient) Attach(ctx context.Context) *connect.BidiStreamForClient[v1.AttachRequest, v1.AttachEvent] {
	return c.attach.CallBidiStream(ctx)
}

// TymuxServiceHandler is an implementation of the tymux.v1.TymuxService service.
type TymuxServiceHandler interface {
	CreateSession(context.Context, *connect.Request[v1.CreateSessionRequest]) (*connect.Response[v1.Session], error)
	ListSessions(context.Context, *connect.Request[v1.ListSessionsRequest]) (*connect.Response[v1.ListSessionsResponse], error)
	KillSession(context.Context, *connect.Request[v1.KillSessionRequest]) (*connect.Response[v1.KillSessionResponse], error)
	// Respawns fresh ptys for a dead-flagged (restored-but-not-yet-revived)
	// session, matching its persisted Layout shape — each pane's original
	// command re-run in its persisted cwd. Never triggered automatically on
	// daemon start (ADR-002); only an explicit call reaches this. A no-op
	// (ReviveSessionResponse.already_live=true) if the session is already
	// live — never a duplicate spawn.
	ReviveSession(context.Context, *connect.Request[v1.ReviveSessionRequest]) (*connect.Response[v1.ReviveSessionResponse], error)
	// One-shot structured read of a pane's current screen — the AI-friendly
	// alternative to `tmux capture-pane` (which returns raw text you'd have
	// to re-parse). Cells carry attributes directly. scrollback_offset lets
	// a caller read a historical screen (copy-mode's navigation primitive)
	// instead of the live one.
	CapturePane(context.Context, *connect.Request[v1.CapturePaneRequest]) (*connect.Response[v1.PaneSnapshot], error)
	// Forward-only substring search through a pane's scrollback — the
	// direct, non-interactive access path for programmatic/agent callers
	// (one shared history buffer, two access paths: this and CapturePane's
	// scrollback_offset).
	SearchScrollback(context.Context, *connect.Request[v1.SearchScrollbackRequest]) (*connect.Response[v1.SearchScrollbackResponse], error)
	// Splits an existing pane into two, nesting a new Split node in its
	// window's Layout tree in the target pane's place. Rejected with
	// failed_precondition (naming the actual/minimum rows+cols) if the
	// resulting leaf would fall below the daemon's hard minimum pane size.
	SplitPane(context.Context, *connect.Request[v1.SplitPaneRequest]) (*connect.Response[v1.Session], error)
	// Closes one pane. If it was the last pane in its window, the window
	// itself closes (cascading to KillSession if it was also the session's
	// last window) — see ClosePaneResponse.window_closed/session_closed.
	ClosePane(context.Context, *connect.Request[v1.ClosePaneRequest]) (*connect.Response[v1.ClosePaneResponse], error)
	// Adds a new window (single pane) to an existing session.
	CreateWindow(context.Context, *connect.Request[v1.CreateWindowRequest]) (*connect.Response[v1.Session], error)
	// Streams the subscribed window's Layout every time its structure or
	// geometry changes, so a client's local model stays in sync without
	// polling ListSessions.
	WatchWindow(context.Context, *connect.Request[v1.WatchWindowRequest], *connect.ServerStream[v1.WindowLayoutEvent]) error
	// Bidirectional stream: client sends input/resize events, server streams
	// output back. This is what an interactive attach (human or agent) uses.
	//
	// Contract:
	//   - The first AttachRequest on the stream MUST set the pane_id field of
	//     the oneof; any other first message is rejected with
	//     invalid_argument.
	//   - Detaching means fully cancelling this call (dropping/closing both
	//     directions). Half-closing only the send side does not stop the
	//     receive side or end the attach — the server keeps streaming output
	//     until the whole call is cancelled.
	//   - output_gap=true on an AttachEvent means the server's internal
	//     output broadcast channel dropped frames for this consumer (it fell
	//     behind); some output between the previous and next Output event was
	//     lost. This is not a fatal condition — normal streaming resumes
	//     immediately after.
	//   - Resize is now window-scoped, not pane-scoped (ADR-004): the
	//     daemon tracks every attached client's reported viewport per window
	//     and applies the dimension-wise minimum across all of them, rather
	//     than sizing this one pane to this one client's last report.
	Attach(context.Context, *connect.BidiStream[v1.AttachRequest, v1.AttachEvent]) error
}

// NewTymuxServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTymuxServiceHandler(svc TymuxServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	tymuxServiceMethods := v1.File_tymux_v1_tymux_proto.Services().ByName("TymuxService").Methods()
	tymuxServiceCreateSessionHandler := connect.NewUnaryHandler(
		TymuxServiceCreateSessionProcedure,
		svc.CreateSession,
		connect.WithSchema(tymuxServiceMethods.ByName("CreateSession")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceListSessionsHandler := connect.NewUnaryHandler(
		TymuxServiceListSessionsProcedure,
		svc.ListSessions,
		connect.WithSchema(tymuxServiceMethods.ByName("ListSessions")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceKillSessionHandler := connect.NewUnaryHandler(
		TymuxServiceKillSessionProcedure,
		svc.KillSession,
		connect.WithSchema(tymuxServiceMethods.ByName("KillSession")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceReviveSessionHandler := connect.NewUnaryHandler(
		TymuxServiceReviveSessionProcedure,
		svc.ReviveSession,
		connect.WithSchema(tymuxServiceMethods.ByName("ReviveSession")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceCapturePaneHandler := connect.NewUnaryHandler(
		TymuxServiceCapturePaneProcedure,
		svc.CapturePane,
		connect.WithSchema(tymuxServiceMethods.ByName("CapturePane")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceSearchScrollbackHandler := connect.NewUnaryHandler(
		TymuxServiceSearchScrollbackProcedure,
		svc.SearchScrollback,
		connect.WithSchema(tymuxServiceMethods.ByName("SearchScrollback")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceSplitPaneHandler := connect.NewUnaryHandler(
		TymuxServiceSplitPaneProcedure,
		svc.SplitPane,
		connect.WithSchema(tymuxServiceMethods.ByName("SplitPane")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceClosePaneHandler := connect.NewUnaryHandler(
		TymuxServiceClosePaneProcedure,
		svc.ClosePane,
		connect.WithSchema(tymuxServiceMethods.ByName("ClosePane")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceCreateWindowHandler := connect.NewUnaryHandler(
		TymuxServiceCreateWindowProcedure,
		svc.CreateWindow,
		connect.WithSchema(tymuxServiceMethods.ByName("CreateWindow")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceWatchWindowHandler := connect.NewServerStreamHandler(
		TymuxServiceWatchWindowProcedure,
		svc.WatchWindow,
		connect.WithSchema(tymuxServiceMethods.ByName("WatchWindow")),
		connect.WithHandlerOptions(opts...),
	)
	tymuxServiceAttachHandler := connect.NewBidiStreamHandler(
		TymuxServiceAttachProcedure,
		svc.Attach,
		connect.WithSchema(tymuxServiceMethods.ByName("Attach")),
		connect.WithHandlerOptions(opts...),
	)
	return "/tymux.v1.TymuxService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TymuxServiceCreateSessionProcedure:
			tymuxServiceCreateSessionHandler.ServeHTTP(w, r)
		case TymuxServiceListSessionsProcedure:
			tymuxServiceListSessionsHandler.ServeHTTP(w, r)
		case TymuxServiceKillSessionProcedure:
			tymuxServiceKillSessionHandler.ServeHTTP(w, r)
		case TymuxServiceReviveSessionProcedure:
			tymuxServiceReviveSessionHandler.ServeHTTP(w, r)
		case TymuxServiceCapturePaneProcedure:
			tymuxServiceCapturePaneHandler.ServeHTTP(w, r)
		case TymuxServiceSearchScrollbackProcedure:
			tymuxServiceSearchScrollbackHandler.ServeHTTP(w, r)
		case TymuxServiceSplitPaneProcedure:
			tymuxServiceSplitPaneHandler.ServeHTTP(w, r)
		case TymuxServiceClosePaneProcedure:
			tymuxServiceClosePaneHandler.ServeHTTP(w, r)
		case TymuxServiceCreateWindowProcedure:
			tymuxServiceCreateWindowHandler.ServeHTTP(w, r)
		case TymuxServiceWatchWindowProcedure:
			tymuxServiceWatchWindowHandler.ServeHTTP(w, r)
		case TymuxServiceAttachProcedure:
			tymuxServiceAttachHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTymuxServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTymuxServiceHandler struct{}

func (UnimplementedTymuxServiceHandler) CreateSession(context.Context, *connect.Request[v1.CreateSessionRequest]) (*connect.Response[v1.Session], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.CreateSession is not implemented"))
}

func (UnimplementedTymuxServiceHandler) ListSessions(context.Context, *connect.Request[v1.ListSessionsRequest]) (*connect.Response[v1.ListSessionsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.ListSessions is not implemented"))
}

func (UnimplementedTymuxServiceHandler) KillSession(context.Context, *connect.Request[v1.KillSessionRequest]) (*connect.Response[v1.KillSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.KillSession is not implemented"))
}

func (UnimplementedTymuxServiceHandler) ReviveSession(context.Context, *connect.Request[v1.ReviveSessionRequest]) (*connect.Response[v1.ReviveSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.ReviveSession is not implemented"))
}

func (UnimplementedTymuxServiceHandler) CapturePane(context.Context, *connect.Request[v1.CapturePaneRequest]) (*connect.Response[v1.PaneSnapshot], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.CapturePane is not implemented"))
}

func (UnimplementedTymuxServiceHandler) SearchScrollback(context.Context, *connect.Request[v1.SearchScrollbackRequest]) (*connect.Response[v1.SearchScrollbackResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.SearchScrollback is not implemented"))
}

func (UnimplementedTymuxServiceHandler) SplitPane(context.Context, *connect.Request[v1.SplitPaneRequest]) (*connect.Response[v1.Session], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.SplitPane is not implemented"))
}

func (UnimplementedTymuxServiceHandler) ClosePane(context.Context, *connect.Request[v1.ClosePaneRequest]) (*connect.Response[v1.ClosePaneResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.ClosePane is not implemented"))
}

func (UnimplementedTymuxServiceHandler) CreateWindow(context.Context, *connect.Request[v1.CreateWindowRequest]) (*connect.Response[v1.Session], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.CreateWindow is not implemented"))
}

func (UnimplementedTymuxServiceHandler) WatchWindow(context.Context, *connect.Request[v1.WatchWindowRequest], *connect.ServerStream[v1.WindowLayoutEvent]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.WatchWindow is not implemented"))
}

func (UnimplementedTymuxServiceHandler) Attach(context.Context, *connect.BidiStream[v1.AttachRequest, v1.AttachEvent]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("tymux.v1.TymuxService.Attach is not implemented"))
}
